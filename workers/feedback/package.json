{
  "name": "hyperliquid-feedback-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply hyperliquid_feedback --local",
    "db:migrate": "wrangler d1 migrations apply hyperliquid_feedback --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260513.0",
    "wrangler": "^4.15.0"
  }
}
